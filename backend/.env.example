# Node Environment
NODE_ENV=development

# Server Configuration
PORT=5000

# CORS Configuration
# Development: http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# OpenAI Configuration
# Required: Your OpenAI API key
OPENAI_API_KEY=sk-your-api-key-here

# Optional: OpenAI model to use (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Session Configuration
# Required: Secret key for session encryption
# Generate a strong random string for production
SESSION_SECRET=your-session-secret-key-change-in-production

# Credential Encryption
# Required: Key for encrypting OAuth tokens and other sensitive credentials
# Should be a strong random string (32+ characters recommended)
# If not set, SESSION_SECRET will be used as fallback
CREDENTIAL_ENCRYPTION_KEY=your-encryption-key-change-in-production

# Atlassian OAuth Configuration
# Required for Jira integration
# Get these from your Atlassian app: https://developer.atlassian.com/console/myapps/
ATLASSIAN_CLIENT_ID=your-atlassian-client-id
ATLASSIAN_CLIENT_SECRET=your-atlassian-client-secret

# OAuth Redirect URI
# Must match the redirect URI configured in your Atlassian OAuth app
# Development: http://localhost:5000/api/jira/oauth/callback
# Production: https://your-domain.com/api/jira/oauth/callback
OAUTH_REDIRECT_URI=http://localhost:5000/api/jira/oauth/callback
