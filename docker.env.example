# Docker Compose Environment Variables
# Copy this file to docker.env and customize as needed
# docker.env is git-ignored and will not be committed

# Backend port mapping (host:container)
# Format: "HOST_PORT:5000" where 5000 is the container's internal port
# Default: 5000 (if not set, docker-compose will use 5000)
BACKEND_PORT=5000

# Frontend port mapping (host:container)
# For development: "HOST_PORT:3000" where 3000 is the container's internal port
# For production: "HOST_PORT:80" where 80 is the container's internal port
# Default: 3000 for dev, 80 for prod (if not set, docker-compose will use defaults)
FRONTEND_PORT=3000
FRONTEND_PORT_PROD=80

# ============================================================================
# Note: Jira Integration Environment Variables
# ============================================================================
# The following environment variables should be configured in your backend
# environment files (backend/.env.development or backend/.env.production):
#
# JIRA_MCP_SERVER_URL - Remote MCP server URL for Jira integration
#   Default: https://mcp.atlassian.com/v1/mcp
#   Example: https://mcp.atlassian.com/v1/mcp
#
# ATLASSIAN_CLIENT_ID - OAuth client ID for Atlassian/Jira authentication
#   Required for Jira OAuth flow
#   Get this from: https://developer.atlassian.com/console/myapps/
#
# ATLASSIAN_CLIENT_SECRET - OAuth client secret for Atlassian/Jira authentication
#   Required for Jira OAuth flow
#   Get this from: https://developer.atlassian.com/console/myapps/
#
# OAUTH_REDIRECT_URI - OAuth callback URL
#   Default: http://localhost:5000/api/jira/oauth/callback
#   Must match the redirect URI configured in your Atlassian OAuth app
#   For production: https://your-domain.com/api/jira/oauth/callback
#
# SESSION_SECRET - Secret key for session encryption
#   Required for secure session management
#   Generate a strong random string (e.g., using openssl rand -hex 32)
#   Important: Use different secrets for development and production
#
# CREDENTIAL_ENCRYPTION_KEY - Key for encrypting OAuth tokens in session
#   Optional: Falls back to SESSION_SECRET if not set
#   Generate a strong random string (e.g., using openssl rand -hex 32)
#   Important: Use different keys for development and production
#
# See docs/JIRA_SETUP.md for detailed setup instructions.
